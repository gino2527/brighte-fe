// own components
import LoadingIcon from '../icons/LoadingIcon';

export const LOADER_CONTAINER_CLASS = 'loader__container';

const Loader = () => (
  <div className="loader">
    <LoadingIcon />
  </div>
);

export default Loader;
